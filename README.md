# innovation_radar

Open `index.html` from the repository root in a browser to view the generic innovation radar. Keep `index.html`, `styles.css`, and `script.js` together in that same root folder, because the page loads the stylesheet and script through relative paths. The page is intentionally self-contained and uses only relative assets, so direct file access (without a local web server) is supported and was verified for this repository.
