# innovation_radar

Open `index.html` from the repository root in a browser to view the generic innovation radar. The page is intentionally self-contained and uses only relative assets, so direct file access (without a local web server) is supported and was verified for this repository.
